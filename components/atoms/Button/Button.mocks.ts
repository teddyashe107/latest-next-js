import { IButton } from ".";


const primary: IButton = {
  variant: 'primary',
  size: 'medium',
  children: 'hello',
};

const secondary: IButton = {
  variant: 'secondary',
  size: 'medium',
  children: 'hello',
};

const large: IButton = {
  variant: 'primary',
  size: 'large',
  children: 'hello',
};

const medium: IButton = {
  variant: 'primary',
  size: 'medium',
  children: 'hello',
};

const small: IButton = {
  variant: 'primary',
  size: 'small',
  children: 'hello',
};

export const mockButtonProps = {
  primary,
  secondary,
  large,
  medium,
  small
};
